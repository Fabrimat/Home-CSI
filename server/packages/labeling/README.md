# @homecsi/labeling

Owned by brief B4 (features/occupancy). Implements ground-truth label
session management and training-data export (see `docs/roadmap.md`,
"Trained-model inference"). See `server/packages/cli/CONTRACTS.md` for
this package's exact exported function contracts (`runLabelCli`,
`runTrain`).
