# @homecsi/features

Owned by brief B4 (features/occupancy). Implements windowed amplitude
feature extraction from `csi_records`. See
`server/packages/cli/CONTRACTS.md` for this package's exact exported
function contract (`runFeaturePipeline`).
